/*#pragma once
#include <SFML/time.hpp>

namespace MNPCore {
    class GameState {
        bool isDead;

    public:
        GameState();
        virtual ~GameState();

        virtual void onEnter() = 0;
        virtual void update(sf::Time &time) = 0;
        virtual void onExit() = 0;
    };
}
*/
